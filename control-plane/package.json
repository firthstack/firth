{
  "name": "firth-control-plane",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "dev": "tsx --env-file-if-exists=.env src/index.ts",
    "live:neon": "tsx --env-file-if-exists=.env scripts/live-neon-check.ts",
    "live:fly": "tsx --env-file-if-exists=.env scripts/live-fly-check.ts",
    "live:tigris": "tsx --env-file-if-exists=.env scripts/live-tigris-check.ts",
    "live:deploy": "tsx --env-file-if-exists=.env scripts/live-deploy-check.ts"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@insforge/sdk": "latest",
    "aws4fetch": "^1.0.20",
    "fastify": "^4.28.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
